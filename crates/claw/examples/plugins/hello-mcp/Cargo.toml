[package]
name = "hello-mcp"
version = "0.1.0"
edition = "2021"
description = "MCP server example: a simple greeting tool"

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
